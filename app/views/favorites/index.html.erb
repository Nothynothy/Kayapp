  <div class="container">

    <div class='fav-title'>
      <h2>Vos favoris (<%= @favorites.count == 0 ? 0 : @favorites.count %>)</h2>
    </div>


    <div class='fav-river-title'>
      <% @rivers_fav.sort_by(&:name).each do |river| %>
        <h3><%= river.name.capitalize %></h3>
      <% end %>
    </div>

    <%# LIST TOPOS %>
    <% if @presence%> <%# ajouter le tri par rivière, avec @rivers_fav %>
      <%= render "shared/topos-cards", topos: @topos_fav %>
    <% else %>
    <div id="fav-false">
      <p id="fav-message"> Pas encore de topos préféré 💔 </p>
        <div id="fav-gif">
          <iframe src="https://giphy.com/embed/3o6MbbcFfp6DGDUiPK" width="100%" height="100%" style="position:absolute"
                  frameBorder="0" class="giphy-embed" allowFullScreen>
          </iframe>
        </div>
    </div>
    <% end %>
  </div>
